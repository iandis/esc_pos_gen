name: esc_pos_gen
description: Declarative-style Dart classes for ESC/POS printing. Ticket class generates ESC/POS commands that can be sent to a thermal printer.
version: 1.0.0
homepage: https://github.com/iandis/esc_pos_gen
issue_tracker: https://github.com/iandis/esc_pos_gen/issues

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hex: ^0.2.0
  html: ^0.15.0
  image: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.8.1

flutter:
  assets:
    - resources/capabilities.json
